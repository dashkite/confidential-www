name: isBytes
fragment: is-bytes
signatures:
  - inputs:
      - value
    output:
      - is-bytes
variables:
  value:
    type: Value
    description: |
      A value to be tested.
  is-bytes:
    type: '[`Boolean`]()'
    description: |
      `true` if `value` is an instance of [`Uint8Array`](), `false` otherwise.
