name: randomBytes
fragment: random-bytes
signatures:
  - inputs:
      - length
    output: bytes
variables:
  length:
    type: '[`Integer`][]'
    description: |
      Length of the desired output, in bytes.
  bytes:
    isPromised: true
    type: '[`Uint8Array`][]'
    description: |
      Promise that resolves to an array of `length` pseudo-random bytes.
