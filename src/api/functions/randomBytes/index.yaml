name: randomBytes
summary: Generates an array of `length` pseudo-random bytes of cryptographic quality.
signatures:
  - arguments: length
    result: bytes
variables:
  length:
    type: '[`Integer`][]'
    description: |
      Length of the desired output, in bytes.
  bytes:
    isPromised: true
    type: '[`Uint8Array`][]'
    description: |
      Promise that resolves to an array of `length` pseudo-random bytes.
