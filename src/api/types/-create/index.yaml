# key-pair create - other create methods are unique to each type
scope: class
category: method
summary: |
  Generates a new instance of [`{{name}}`][] containing a pair of algorithmically related keys using the [`randomBytes`][] interface.
signatures:
  - arguments:
    result: {{name|dashed}}
variables:
  - name: {{name|dashed}}
    type: '[`Promise`][] for an [`{{name}}`][]'
    description: |
      Promise that resolves to an instance of [`{{name}}`][].
