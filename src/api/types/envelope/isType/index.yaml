signatures:
  - inputs:
      - value
    output: is-envelope
variables:
  - name: value
    type: Value
    description: |
      A value to be tested.
  - name: is-envelope
    type: '[`Boolean`][]'
    description: |
      `true` if `value` is an instance of `Envelope`, `false` otherwise.
