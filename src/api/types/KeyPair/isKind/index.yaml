category: method
scope: class
signatures:
  - arguments: value
    result: is-key-pair
variables:
  - name: value
    type: Value
    description: |
      A value to be tested.
  - name: is-key-pair
    type: '[`Boolean`][]'
    description: >
      true` if `value` has an instance of `KeyPair` in its prototype chain,
      `false` otherwise.
