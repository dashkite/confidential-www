signatures:
  - inputs:
      - value
    output: is-hash
variables:
  - name: value
    type: Value
    description: |
      A value to be tested.
  - name: is-hash
    type: '[`Boolean`][]'
    description: |
      `true` if `value` is an instance of `Hash`, `false` otherwise.
