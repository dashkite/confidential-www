name: isType
category: method
scope: class
summary: Returns `true` if the input `value` is an instance of `Declaration`.
signatures:
  - arguments: value
    result: is-declaration
variables:
  value:
    type: Value
    description: |
      A value to be tested.
  is-declaration:
    type: '[`Boolean`][]'
    description: |
      `true` if `value` is an instance of `Declaration`, `false` otherwise.
