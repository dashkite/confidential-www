category: method
scope: instance
summary: Converts this instance's value into the form specified by `hint`.
signatures:
  - arguments: hint
    result: encoded-declaration
variables:
  - name: hint
    type: '[`String`][]'
    description: |
      name that specifies the `to` conversion hint.
  - name: encoded-declaration
    type: |
      - [`String`][]
      - [`Uint8Array`][]
    description: |
      A data representation of a declaration.
