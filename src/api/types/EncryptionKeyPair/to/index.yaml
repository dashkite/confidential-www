category: method
scope: instance
signatures:
  - arguments: hint
    result: encoded-envelope
variables:
  - name: hint
    type: '[`String`][]'
    description: |
      name that specifies the `to` conversion hint.
  - name: encoded-envelope
    type: |
      - [`String`][]
      - [`Uint8Array`][]
    description: |
      A data representation of an envelope.
