name: isType
scope: class
category: method
signatures:
  - arguments: value
    result: is-encryption-key-pair
variables:
  - name: value
    type: Value
    description: |
      A value to be tested.
  - name: is-encryption-key-pair
    type: '[`Boolean`][]'
    description: >
      `true` if `value` is an instance of [`EncryptionKeyPair`][], `false`
      otherwise.
