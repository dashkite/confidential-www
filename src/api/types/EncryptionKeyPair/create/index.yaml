scope: class
category: method
signatures:
  - arguments:
    result: encryption-key-pair
variables:
  - name: encryption-key-pair
    isPromised: true
    type: '[`EncryptionKeyPair`][]'
    description: |
      Promise that resolves to an instance of [`EncryptionKeyPair`][].
