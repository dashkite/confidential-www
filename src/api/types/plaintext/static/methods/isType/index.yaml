signatures:
  - inputs:
      - value
    output: is-plaintext
variables:
  - name: value
    type: Value
    description: |
      A value to be tested.
  - name: is-plaintext
    type: '[`Boolean`][]'
    description: |
      `true` if `value` is an instance of `Plaintext`, `false` otherwise.
