signatures:
  - inputs:
      - value
    output: is-private-key
variables:
  - name: value
    type: Value
    description: |
      A value to be tested.
  - name: is-private-key
    type: '[`Boolean`][]'
    description: |
      `true` if `value` is an instance of [`PrivateKey`][], `false` otherwise.
