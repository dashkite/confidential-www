category: method
scope: class
signatures:
  - arguments: value
    result: is-symmetric-key
variables:
  - name: value
    type: Value
    description: |
      A value to be tested.
  - name: is-symmetric-key
    type: '[`Boolean`][]'
    description: |
      `true` if `value` is an instance of [`SymmetricKey`][], `false` otherwise.
