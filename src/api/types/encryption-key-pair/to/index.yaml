scope: instance
category: method
signatures:
  - inputs:
      - hint
    output: encoded-envelope
variables:
  - name: hint
    type: '[`String`][]'
    description: |
      name that specifies the `to` conversion hint.
  - name: encoded-envelope
    type: '[`String`][] | [`Uint8Array`][]'
    description: |
      A data representation of an envelope.
