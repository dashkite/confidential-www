category: method
scope: instance
signatures:
  - arguments: hint
    result: encoded-key
variables:
  - name: hint
    type: '[`String`][]'
    description: |
      name that specifies the `to` conversion hint.
  - name: encoded-key
    type: '[`String`][] | [`Uint8Array`][]'
    description: |
      A data representation of a key
