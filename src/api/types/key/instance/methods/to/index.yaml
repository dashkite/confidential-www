signatures:
  - inputs:
      - hint
    output: encoded-key
variables:
  hint:
    type: '[`String`]()'
    description: |
      name that specifies the `to` conversion hint.
  encoded-key:
    type: '[`String`]() | [`Uint8Array`]()'
    description: |
      A data representation of a key
