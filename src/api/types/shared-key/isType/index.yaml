signatures:
  - inputs:
      - value
    output: is-shared-key
variables:
  - name: value
    type: Value
    description: |
      A value to be tested.
  - name: is-shared-key
    type: '[`Boolean`][]'
    description: |
      `true` if `value` is an instance of [`SharedKey`][], `false` otherwise.
