category: method
scope: instance
signatures:
  - arguments: hint
    result: encoded-nonce
variables:
  - name: hint
    type: '[`String`][]'
    description: |
      name that specifies the `to` conversion hint.
  - name: encoded-nonce
    type: '[`String`][] | [`Uint8Array`][]'
    description: |
      A data representation of a nonce
