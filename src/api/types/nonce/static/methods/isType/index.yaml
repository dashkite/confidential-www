signatures:
  - inputs:
      - value
    output: is-nonce
variables:
  value:
    type: Value
    description: |
      A value to be tested.
  is-nonce:
    type: '[`Boolean`]()'
    description: |
      `true` if `value` is an instance of `Nonce`, `false` otherwise.
