category: method
scope: class
signatures:
  - arguments: value
    result: is-nonce
variables:
  - name: value
    type: Value
    description: |
      A value to be tested.
  - name: is-nonce
    type: '[`Boolean`][]'
    description: |
      `true` if `value` is an instance of `Nonce`, `false` otherwise.
