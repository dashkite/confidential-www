body
  height 100vh
  display grid
  grid-template-rows auto 1fr auto
  grid-template-columns 100vw
  margin 0
  padding 0

  & > main
    overflow-y scroll

row(-wrap)
  display flex
  flex-flow row -wrap

column(-wrap)
  display flex
  flex-flow column -wrap
