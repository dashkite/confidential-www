title: SignatureKeyPair
prototype: "[`KeyPair`]()"
properties: {}
prose: |
  Key pair suitable for [digital signing](). You may generate a pair by invoking [`keyPair.signature`]().

  > **Warning:** `SignatureKeyPair` is not suitable for encryption.
