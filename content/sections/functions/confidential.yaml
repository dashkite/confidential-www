title: confidential
signature:
  inputs: [ randomBytes& ]
  output: api
  variables:
    randomBytes:
      type: function
      description: |
        Provide a custom implementation for [randomBytes][randombytes].

        > Warning: A robust source of entropy is necessary for encryption. Use this feature with caution.
    api:
      type: object
      description: |
        An instance of the Confidential API.

prose: |
  An instance of the Confidential API.

examples: []
