#!/usr/bin/env bash

# (cd src/api/types/Declaration && rm -rf from && ln -s ../-from from)
# (cd src/api/types/Declaration && ln -s ../-to to)
# (cd src/api/types/Declaration && ln -s ../-isType isType)
#
# (cd src/api/types/EncryptionKeyPair && rm -rf from && ln -s ../-from from)
# (cd src/api/types/EncryptionKeyPair && rm -rf to && ln -s ../-to to)
# (cd src/api/types/EncryptionKeyPair && rm -rf isType && ln -s ../-isType isType)
#
# (cd src/api/types/Envelope && rm -rf from && ln -s ../-from from)
# (cd src/api/types/Envelope && rm -rf isType && ln -s ../-isType isType)
#
# (cd src/api/types/Hash && rm -rf from && ln -s ../-from from)
# (cd src/api/types/Hash && rm -rf to && ln -s ../-to to)
# (cd src/api/types/Hash && rm -rf isType && ln -s ../-isType isType)
#
# (cd src/api/types/Key && rm -rf to && ln -s ../-to to)
#
# (cd src/api/types/KeyPair && rm -rf to && ln -s ../-to to)
#
# (cd src/api/types/Nonce && rm -rf from && ln -s ../-from from)
# (cd src/api/types/Nonce && rm -rf to && ln -s ../-to to)
# (cd src/api/types/Nonce && rm -rf isType && ln -s ../-isType isType)
#
# (cd src/api/types/Plaintext && rm -rf from && ln -s ../-from from)
# (cd src/api/types/Plaintext && rm -rf to && ln -s ../-to to)
# (cd src/api/types/Plaintext && rm -rf isType && ln -s ../-isType isType)
#
# (cd src/api/types/PrivateKey && rm -rf from && ln -s ../-from from)
# (cd src/api/types/PrivateKey && rm -rf isType && ln -s ../-isType isType)
#
# (cd src/api/types/PublicKey && rm -rf from && ln -s ../-from from)
# (cd src/api/types/PublicKey && rm -rf isType && ln -s ../-isType isType)
#
# (cd src/api/types/SharedKey && rm -rf from && ln -s ../-from from)
# (cd src/api/types/SharedKey && rm -rf isType && ln -s ../-isType isType)
#
# (cd src/api/types/SignatureKeyPair && rm -rf from && ln -s ../-from from)
# (cd src/api/types/SignatureKeyPair && rm -rf isType && ln -s ../-isType isType)
#
# (cd src/api/types/SymmetricKey && rm -rf from && ln -s ../-from from)
# (cd src/api/types/SymmetricKey && rm -rf isType && ln -s ../-isType isType)
