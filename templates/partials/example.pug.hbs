h3{{slug}}
  a.header-anchor(href="{{slug}}") {{title}}

{{#if content}}
:markdown
  {{indent 2 (setLinks content)}}
{{/if}}

{{#if note}}
blockquote
  :markdown
    {{indent 4 (setLinks note)}}
{{/if}}
