::
$fragments.prose = (content) ->
  $bind """
  \n:markdown
    #{indent 2, content}
  """

$fragments.blockquote = (content) ->
  $bind """
  \nblockquote
    :markdown
      #{indent 4, content}
  """
::
