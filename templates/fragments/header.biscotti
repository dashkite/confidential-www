::
$fragments.linkedHeader = (level, content) ->
  anchor = "#" + $string(content).slugify().s
  $bind """
  \nh#{level}#{anchor}
    a.header-anchor(href="#{anchor}") #{content}
  """

$fragments.header = (level, content) ->
  $bind """
  \nh#{level} #{content}
  """
::
