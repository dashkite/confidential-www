section
  h2#function-::$bind title.replace(/\./g, "-")::
    a.header-anchor(href="#function-::$bind title.replace /\./, "-"::") ::$bind title::

  ::
  $fragments.signature title, signature
  $fragments.signatureTable variables
  $fragments.prose prose ? ""
  for example in examples
    $fragments.example example
  ::
