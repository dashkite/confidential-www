section
  h2 ::$bind title::

  ::
  $signature title, signature.inputs, signature.output
  ::

  ::
  $table ["name", "type", "description"],
    for name, {type, description} of signature.variables
      [name, type, description]
  ::
