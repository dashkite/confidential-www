section
  h2#function-{{slugify title}}
    a.header-anchor(href="#function-{{slugify title}}") {{title}}

  {{#each signature}}
  {{> signature .}}
  {{/each}}

  {{> table table}}

  {{#if prose}}
  :markdown
    {{indent 4 prose}}
  {{/if}}

  {{#each examples}}
  {{> example .}}
  {{/each}}
